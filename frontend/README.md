# Frontend (React + Vite)

## Scripts

```bash
npm install
npm run dev
npm run build
npm run preview
npm run lint
```

## Env

Tạo `.env` (hoặc dùng biến môi trường) với:

```bash
VITE_API_BASE_URL=http://localhost:8080
```

Frontend sẽ gọi `${VITE_API_BASE_URL}/api/hello`.
